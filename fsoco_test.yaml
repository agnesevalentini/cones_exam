# all this script is copy pasted fron the ultralytics website

# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# COCO8 dataset (first 8 images from COCO train2017) by Ultralytics
# Documentation: https://docs.ultralytics.com/datasets/detect/coco8/
# Example usage: yolo train data=coco8.yaml
# parent
# ├── ultralytics
# └── datasets
#     └── coco8 ← downloads here (1 MB)

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: amz # dataset root dir, example with just 'amz' as directory
train: images # train images (relative to 'path') 4 images
val: images # val images (relative to 'path') 4 images
test: # test images (optional)

# Classes
names:
    0: yellow_cone
    1: blue_cone
    2: orange_cone
    #3: seg_blue_cone
    #4: orange_cone
    #5: seg_orange_cone
    #6: large_orange_cone
    #7: seg_large_orange_cone
    #8: unknown_cone
    #9: seg_unknown_cone
# Download script/URL (optional)
# download: https://github.com/ultralytics/assets/releases/download/v0.0.0/coco8.zip